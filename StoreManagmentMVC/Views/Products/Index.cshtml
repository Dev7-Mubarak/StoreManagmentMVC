
<a type="button" class="btn btn-success" asp-controller="Products" asp-action="Create">Add Product</a>

@* @model IEnumerable<Product>

<div class="col-lg-11 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Products</h4>

            <p class="card-description">
                <a type="button" class="btn btn-success" asp-controller="Products" asp-action="Create">Add Product</a>
            </p>

            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Product Name</th>
                            <th>Description</th>
                            <th>Price</th>
                            <th>Stock Quantity</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var product in Model)
                        {
                            <tr>
                                <td>@product.Name</td>
                                <td>@product.Description</td>
                                <td>@product.Price</td>
                                <td>@product.StockQuantity</td>
                                <td>
                                    <label class="badge @(product.IsActive ? "badge-success" : "badge-danger")">
                                        @(product.IsActive ? "Active" : "Inactive")
                                    </label>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
 *@